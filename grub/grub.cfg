# GRUB configuration: copied to /boot/grub/grub.cfg inside the ISO.
# The 'multiboot' command loads the kernel using the multiboot 1 protocol.

set timeout=0
set default=0

menuentry "KFS-2" {
	multiboot /boot/kernel.bin
	boot
}
